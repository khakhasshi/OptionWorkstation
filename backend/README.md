# Legacy Python Reference

This directory is retained only for output-parity checks during the Rust migration. It is not started by `scripts/start.sh`, not used by the frontend, and must not receive credentials.
